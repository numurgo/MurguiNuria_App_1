<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Controles">

    <ImageButton
        android:id="@+id/ibRight"
        style="@style/botonControles"
        android:layout_marginTop="156dp"
        android:layout_marginEnd="112dp"
        android:background="@drawable/round_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/rightarrow" />

    <ImageButton
        android:id="@+id/ibLeft"
        style="@style/botonControles"
        android:layout_marginStart="112dp"
        android:layout_marginTop="156dp"
        android:background="@drawable/round_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/leftarrow" />

    <ImageButton
        android:id="@+id/ibTop"
        style="@style/botonControles"
        android:layout_marginStart="180dp"
        android:layout_marginTop="90dp"
        android:layout_marginEnd="180dp"
        android:background="@drawable/round_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ibDown"
        app:srcCompat="@drawable/downarrow" />

    <ImageButton
        android:id="@+id/ibDown"
        style="@style/botonControles"
        android:layout_marginStart="180dp"
        android:layout_marginTop="90dp"
        android:layout_marginEnd="180dp"
        android:background="@drawable/round_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/uparrow" />

    <Button
        android:id="@+id/bNivelSuperado"
        style="@style/button"
        android:layout_marginEnd="48dp"
        android:layout_marginBottom="48dp"
        android:text="Misión superada"
        android:padding="5dp"
        android:onClick="nivelSuperado"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>